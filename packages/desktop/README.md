# @prismalens/desktop

The Electron launcher for PrismaLens: it spawns the same backend `pl up` runs, opens a window
on it, and adds a tray and a notification when an investigation finishes.

Documentation: https://docs.prismalens.io

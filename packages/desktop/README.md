# @prismalens/desktop

The Electron launcher for PrismaLens: it spawns the same backend `pl up` runs, declares the
laptop placement, opens a window on it, and adds a tray and a notification when an
investigation finishes. It never embeds the API (ADR 0005 §8).

Documentation: https://docs.prismalens.io

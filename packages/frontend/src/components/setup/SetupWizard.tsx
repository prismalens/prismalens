'use client';

import React from 'react';

export interface SetupWizardProps {
  // TODO: Define props during implementation
  // currentStep?: number;
  // onComplete?: () => void;
}

export function SetupWizard({}: SetupWizardProps) {
  return (
    <div>
      {/* TODO: Implement SetupWizard */}
    </div>
  );
}

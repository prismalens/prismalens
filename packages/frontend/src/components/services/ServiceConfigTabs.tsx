'use client';

import React from 'react';

export interface ServiceConfigTabsProps {
  // TODO: Define props during implementation
  // activeTab?: 'general' | 'integrations' | 'investigation' | 'dependencies';
  // onTabChange?: (tab: string) => void;
}

export function ServiceConfigTabs({}: ServiceConfigTabsProps) {
  return (
    <div>
      {/* TODO: Implement ServiceConfigTabs */}
    </div>
  );
}

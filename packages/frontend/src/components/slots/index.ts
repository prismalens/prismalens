/**
 * Extension Slot Components
 *
 * These components provide UI slots where enterprise extensions
 * can inject their components.
 */

export {
  ExtensionSlot,
  useSlotComponents,
  useIsEnterprise,
  useEdition,
} from './ExtensionSlot';

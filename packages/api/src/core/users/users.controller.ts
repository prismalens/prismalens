import { Controller } from "@nestjs/common";

/**
 * Users Controller
 *
 * Note: Setup endpoints have been moved to SetupController (oRPC).
 * This controller is reserved for future user management endpoints.
 */
@Controller("users")
export class UsersController {
	// Future user management endpoints will be added here
}

{
	"name": "@prismalens/engine",
	"version": "0.2.1",
	"private": true,
	"description": "Two-tier AI investigation engine for PrismaLens \u2014 a supervisor that rents a coding-agent harness and reduces its evidence into an ordered-hypothesis report",
	"license": "Apache-2.0",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"NOTICE"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/prismalens/prismalens.git",
		"directory": "packages/@prismalens/engine"
	},
	"homepage": "https://prismalens.io",
	"bugs": "https://github.com/prismalens/prismalens/issues",
	"keywords": [
		"sre",
		"incident-response",
		"root-cause-analysis",
		"investigation",
		"ai",
		"agent"
	],
	"engines": {
		"node": ">=22"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest run && node --test scripts/refusal-gate-check.test.mjs",
		"test:watch": "vitest",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@prismalens/config": "workspace:*",
		"@prismalens/contracts": "workspace:*",
		"zod": "catalog:"
	},
	"//peerDependencies": "`e2b` is the cloud `enforced` sandbox provider (ADR-0020). src/sandbox/e2b.ts resolves it lazily through createRequire and refuses with an actionable message when it is absent, so it must NOT be a hard dependency \u2014 but it must be DECLARED, or the packaging import scan (scripts/pack-cli.mjs) cannot tell a deliberate optional apart from a missed hoist.",
	"peerDependencies": {
		"e2b": "*"
	},
	"peerDependenciesMeta": {
		"e2b": {
			"optional": true
		}
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}

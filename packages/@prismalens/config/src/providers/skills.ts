// Skills provider metadata is currently defined entirely within env/skills.ts
// as environment variable schemas. This file exists as a placeholder for
// future static skill metadata (browser-safe, no env dependencies).
//
// When skill identity metadata is needed (similar to agents/llm/mcp providers),
// define it here and keep env validation in env/skills.ts.

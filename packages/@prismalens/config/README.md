# @prismalens/config

@prismalens/config manages configuration resolution, environment schema validation, and infrastructure metadata for PrismaLens. It handles settings hierarchy resolution, credential resolution for model providers, and agent and tool definitions across client and server environments. The package ensures consistent configuration state across all runtime components.

Product documentation and configuration references are available at https://docs.prismalens.io.

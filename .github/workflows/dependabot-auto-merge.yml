# This is a managed caller stub.
# Logic lives in prismalens/gh-workflows/.github/workflows/dependabot-auto-merge.yml.
# Do not add logic here.

name: Dependabot auto-merge

on: pull_request

jobs:
  auto-merge:
    uses: prismalens/gh-workflows/.github/workflows/dependabot-auto-merge.yml@main
    permissions:
      contents: write
      pull-requests: write

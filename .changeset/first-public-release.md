---
"prismalens": patch
"@prismalens/engine": patch
"@prismalens/contracts": patch
"@prismalens/config": patch
---

First public release. The `prismalens` CLI (bins `prismalens` + `pl`) and its
library closure — `@prismalens/engine`, `@prismalens/contracts`,
`@prismalens/config` — publish to npm as 0.0.1 under Apache-2.0.

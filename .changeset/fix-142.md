---
"@prismalens/engine": patch
"@prismalens/config": patch
"prismalens": patch
---

* feat(engine)!: `SynthesisModelConfig` now requires `configured: boolean`.
* fix: Tier-1 credential resolution now obeys the strict `env -> _FILE -> none` hierarchy for all LLM providers.
* fix(cli): `pl listen` no longer crashes if a Tier-1 provider is not configured.
* fix(cli): `pl doctor` explicitly skips connection checks when running with `--no-ping`.

---
"prismalens": minor
---

You can now map services directly to local git checkouts on the worker machine under Service detail → Repositories. Investigations automatically run inside the mapped repository directory rather than falling back to the worker process's working directory.

---
"prismalens": patch
---

The `/api/openapi.json` endpoint and OpenAPI generation script now correctly emit JSON schemas for request bodies and dynamic path parameters rather than empty schemas or generation errors. (#547)

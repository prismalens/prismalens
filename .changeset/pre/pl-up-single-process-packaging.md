---
"prismalens": minor
---

`pl up` now runs the complete application—API server, web dashboard, SQLite database, and migrations—as a single process on one port from one npm install. No Docker or Redis containers are required.

Action: Node.js 24 or newer is required to run `prismalens`.

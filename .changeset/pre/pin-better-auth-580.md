---
"prismalens": patch
---

Account setup now logs unexpected errors during initial owner creation and pins authentication libraries to prevent schema validation failures during first-run setup. (#580)

---
"prismalens": patch
---

The investigation stream panel now groups events into collapsible branches only when multiple branches exist, and autoscroll pauses when you scroll up to inspect earlier logs. Navigating between investigations now immediately clears the previous run's stream events. (#280)

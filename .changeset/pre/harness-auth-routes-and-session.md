---
"prismalens": patch
---

Investigation harnesses can now authenticate using active local CLI sessions (such as `claude` CLI logins) without requiring an API key. Settings now displays real-time harness readiness and diagnostic status for available harnesses.

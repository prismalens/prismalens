---
"prismalens": patch
---

When a live investigation stream disconnects, the progress card now displays an explicit polling indicator while continuing in the background. In-stream errors display directly in the active panel, and failed runs show a clear failure header. (#462)

---
"prismalens": patch
---

Keyless providers like local Ollama can now run investigations without requiring an API key. Auto-selection can use a signed-in Claude Code CLI session regardless of the active synthesis provider, and custom models are only forwarded to compatible harnesses. (#519, #525)

---
"prismalens": minor
---

Per-agent LLM override settings and the agent roster UI are removed from settings rather than relocated, and investigations now use the single active provider configuration. A two-tier-native successor is tracked separately in #130. (#534)

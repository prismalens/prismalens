---
"prismalens": patch
---

Starting an investigation when no harness is runnable is now rejected immediately by the API with an explanation, preventing incidents from getting stuck in pending status. (#520)

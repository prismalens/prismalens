---
"prismalens": patch
---

The unmapped alerts tab and dashboard unassigned alert counts now stay strictly in sync via server-side filtering. The incident detail panel continues displaying the root cause from completed investigations while a subsequent investigation run is in progress.
